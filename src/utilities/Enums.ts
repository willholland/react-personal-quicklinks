export enum Layouts {
    LayoutCompact,
    LayoutFilmstrip,
    LayoutGrid,
    LayoutButton,
    LayoutList,
    LayoutTiles
}

export enum IconsOptions {
    NoIcon,
    IconOnLeft,
    IconOnTop
}

export enum ButtonAppearance {
    NoOutline,
    Outline,
    FillColor,
}

export enum AlignmentOptions {
    Top,
    Center
}

export enum TitleTextOptions {
    OneLine,
    TwoLines,
}

export enum IconSizeOptions {
    Small,
    Medium,
    Large,
    ExtraLarge,
    FillSpace
}

export enum ThumbnailOptions {
    AutoSelected,
    CustomImage,
    Icon
}